30/12/2024
    Générer les compatibilités navigateurs pour le CSS
    Terminer les tests du hook useCalculator



console.log("\u03c0")


When :   pi ( pi ^ 2 )
    bug

Animation when click on a button, with the button translating in x and y while pressed


should return error if trying doing square root on negative number


Code des doubles piles
    //Here is the frontStack, the one the user can see when he types
    const [frontStack, dispatchFrontStack] = useReducer(StackReducer, { stack: []});
    //Here is the backStack, used to calculate the expression
    const [backStack, dispatchBackStack] = useReducer(StackReducer, { stack: []});

    //Old version with one stack